python-dotenv>=1.0.0
fastapi==0.110.0
uvicorn[standard]==0.29.0
psycopg2-binary==2.9.9
pydantic==2.6.4
python-multipart==0.0.9
requests==2.31.0
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
cryptography>=42.0.0    # explicit dep for services/token_crypto.py (Fernet) -- already pulled in transitively by python-jose[cryptography] above
pypdf>=4.0.0
reportlab>=4.2.0
python-docx>=1.1.0
openpyxl>=3.1.0
gtts>=2.4.0
edge-tts>=6.1.9
google-cloud-storage>=2.16.0
boto3>=1.34.0    # S3-compatible object storage (Backblaze B2) -- see services/storage.py
openai>=1.40.0
pymupdf>=1.24.0
mammoth>=1.6.0
watchdog>=4.0.0
arq==0.26.3      # Redis-backed job queue for background workers (see app/worker.py) -- async-native, built-in cron, picked over RQ (sync/fork-based, needs a separate rq-scheduler add-on for cron). Optional: only used when REDIS_URL is set.
redis==5.0.8     # direct dep for services/rate_limit.py's Redis-mode sliding window (also pulled in transitively by arq above)
